//const { default: $ } = require('webdriverio/build/commands/browser/$');
/*
describe ('TestLogout', () => {

    it('TestingLogout', async () => {

        var expect = require('chai').expect;
        
        await browser.url('');

        let username = await $('[id = "username"]');
        await username.setValue('jsmith@demo.io');
        
        let password = await $('[id = "password"]');
        await password.setValue('Demo123!');
        
        let submit = await $('[id = "submit"]');
        await submit.click();

        let avatar = await $('.user-avatar');
        await avatar.click();

        let dropdownMenu = await $('=Logout');
        await dropdownMenu.click();

        await expect(await $('badge badge-pill badge-success')).toExist();

    });

});
*/